rules_version = '2';
service cloud.firestore {
    match /databases/{database}/documents {
        // All writes and reads go through Cloud Functions using the admin SDK,
        // which bypasses these rules. Client SDKs are denied for every path.
        match /{document=**} {
            allow read, write: if false;
        }
    }
}
